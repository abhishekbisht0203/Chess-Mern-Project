<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Real-Time Chess Game</title>
  <link rel="stylesheet" href="/static/style.css" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
    }

    .square {
      width: 60px;
      height: 60px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .light {
      background-color: #f0d9b5;
    }

    .dark {
      background-color: #b58863;
    }

    .piece {
      font-size: 2rem;
      cursor: grab;
      user-select: none;
    }

    .move-dot {
      width: 14px;
      height: 14px;
      background-color: rgba(0, 0, 0, 0.3);
      border-radius: 50%;
    }

    .chessboard {
      display: grid;
      grid-template-columns: repeat(8, 60px);
      grid-template-rows: repeat(8, 60px);
      border: 6px solid #8b5e3c;
      box-shadow: 0 0 20px #0006;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-yellow-100 to-yellow-300 min-h-screen flex flex-col items-center justify-center">

  <div class="text-center mb-4">
    <h2 id="status" class="text-2xl font-bold text-gray-800">Waiting for players...</h2>
    <h3 id="winner" class="text-xl font-bold text-green-600 hidden"></h3>
  </div>

  <div class="flex items-center justify-between w-[480px] mb-2 text-gray-800 font-semibold text-lg">
    <span id="black-timer">Black ⏱️ 10:00</span>
    <span id="white-timer">White ⏱️ 10:00</span>
  </div>

  <div class="chessboard"></div>

  <script src="https://cdn.socket.io/4.8.1/socket.io.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js"></script>
  <script src="/javascripts/script.js"></script>

</body>
</html>
